<windows:NonActivatingWindow x:Class="AiTranslator.App.Windows.BadgeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:windows="clr-namespace:AiTranslator.App.Windows"
        xmlns:res="clr-namespace:AiTranslator.App.Resources"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        SizeToContent="WidthAndHeight">
    <Border x:Name="Root" Width="26" Height="26" CornerRadius="13"
            Background="#FF2D7DF6" BorderBrush="#FF1B5FCB" BorderThickness="1"
            ToolTip="{x:Static res:UiStrings.BadgeTooltip}" Cursor="Hand">
        <TextBlock Text="⇄" Foreground="White" FontSize="15" FontWeight="SemiBold"
                   HorizontalAlignment="Center" VerticalAlignment="Center" />
    </Border>
</windows:NonActivatingWindow>
