<Window x:Class="AiTranslator.App.Windows.OverlayInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:res="clr-namespace:AiTranslator.App.Resources"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        Width="460"
        SizeToContent="Height">
    <Window.Resources>
        <Style x:Key="Primary" TargetType="Button">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="#FF2D7DF6" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="16,6" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bg" Background="{TemplateBinding Background}" CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#FF3A8AFF" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bg" Property="Background" Value="#FF3A3A42" />
                                <Setter Property="Foreground" Value="#FF8A8A93" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IconButton" TargetType="Button">
            <Setter Property="Foreground" Value="#FF9AA4B2" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="FontFamily" Value="Segoe Fluent Icons, Segoe MDL2 Assets" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{TemplateBinding Background}" CornerRadius="5" Padding="5,3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="#FF2C2C34" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="10" Background="#FF1E1E1E" BorderBrush="#FF3A3A3A" BorderThickness="1" Padding="12">
        <DockPanel>
            <DockPanel DockPanel.Dock="Top" Margin="2,0,2,8" LastChildFill="False">
                <TextBlock x:Name="DirectionLabel" DockPanel.Dock="Left" VerticalAlignment="Center"
                           Foreground="#FFB7C7E6" FontSize="11" FontWeight="SemiBold" />
                <Button x:Name="SettingsButton" DockPanel.Dock="Right" Style="{StaticResource IconButton}"
                        Click="OnSettingsClick" ToolTip="{x:Static res:UiStrings.TraySettings}" Content="&#xE713;" />
            </DockPanel>

            <Grid DockPanel.Dock="Bottom" Margin="0,10,0,0">
                <TextBlock x:Name="Status" HorizontalAlignment="Left" VerticalAlignment="Center"
                           Foreground="#FFE06C75" FontSize="11" TextWrapping="Wrap"
                           Visibility="Collapsed" MaxWidth="300" />
                <Button x:Name="TranslateButton" HorizontalAlignment="Right" Style="{StaticResource Primary}"
                        Click="OnTranslateClick" Content="{x:Static res:UiStrings.OverlayTranslate}"
                        ToolTip="Ctrl+Enter" />
            </Grid>

            <TextBox x:Name="Input"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     Background="Transparent"
                     Foreground="White"
                     CaretBrush="White"
                     BorderThickness="0"
                     FontSize="16"
                     Height="130"
                     VerticalScrollBarVisibility="Auto" />
        </DockPanel>
    </Border>
</Window>
